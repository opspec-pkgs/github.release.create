name: test
description: tests the op
inputs:
  srcDir:
    dir:
      default: .
run:
  container:
    image:
      ref: opctl/opctl:0.1.53-dood
    dirs:
      /src: $(srcDir)
    workDir: /src
    cmd: 
      - sh
      - -c
      - opctl op validate test